// Colors extracted directly from the Plan Ore logo.
export const theme = {
  navy: "#293039",       // deep background
  navyLight: "#333c47",  // card / raised surface on navy
  navyLighter: "#3d4650",// hover / border on navy
  mint: "#c3edef",       // soft mint text / accents
  teal: "#00c2cb",       // bright teal — primary accent
  tealDark: "#009aa1",   // teal hover state
  white: "#ffffff",
  ink: "#0f172a",        // near-black for text on light surfaces
  slate: "#64748b",      // muted text on light surfaces
  slateLight: "#94a3b8",
  border: "#e2e8f0",
  surface: "#f8fafc",
  success: "#16a34a",
  successBg: "#dcfce7",
  danger: "#dc2626"
};

export const fonts = {
  display: "'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
  body: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif"
};
